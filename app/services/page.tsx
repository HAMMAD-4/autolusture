import { SiteFooter,SiteNav } from '@/components/site-shell';import Link from 'next/link';import { services } from '@/lib/data';
export default function Services(){return <><SiteNav/><main className="wrap"><div className="page-hero"><div className="eyebrow">The menu</div><h1>Details with <em>purpose.</em></h1><p>Choose a focused refresh or hand us the keys for a deeper transformation. Every detail includes a careful final inspection.</p></div><div className="service-grid" style={{paddingBottom:80}}>{services.map(s=><Link className="service-card" href={`/booking?service=${s.slug}`} key={s.slug}><span>{s.category}</span><h2>{s.name}</h2><p>{s.description}</p><footer><span>From ${s.price}</span><span>{s.duration} →</span></footer></Link>)}</div></main><SiteFooter/></>}
