'use client';
import { Suspense,useState } from 'react';import { useSearchParams,useRouter } from 'next/navigation';import Link from 'next/link';
export default function Login(){return <Suspense><LoginForm/></Suspense>}
function LoginForm(){const search=useSearchParams(),router=useRouter(),[email,setEmail]=useState(search.get('next')?.includes('/rep')?'rep@autolustre.local':'admin@autolustre.local'),[password,setPassword]=useState(''),[error,setError]=useState(''),[loading,setLoading]=useState(false);const submit=async(e:React.FormEvent)=>{e.preventDefault();setLoading(true);setError('');const res=await fetch('/api/auth/login',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({email,password,next:search.get('next')})});const data=await res.json();setLoading(false);if(!res.ok){setError(data.error);return}router.push(data.next);router.refresh()};return <main className="booking-shell"><Link className="brand" href="/">auto<i>lustre</i></Link><form className="form-card" onSubmit={submit} style={{marginTop:30}}><div className="eyebrow">Secure staff access</div><h1>Portal sign in</h1><p>Choose the local Admin or Rep account for testing.</p><div className="fields"><div className="field full"><label>Email</label><input value={email} onChange={e=>setEmail(e.target.value)} type="email" autoComplete="username"/></div><div className="field full"><label>Password</label><input value={password} onChange={e=>setPassword(e.target.value)} type="password" autoComplete="current-password"/></div></div>{error&&<p className="error">{error}</p>}<button className="button dark" style={{marginTop:25}} disabled={loading}>{loading?'Signing in…':'Sign in →'}</button></form></main>}
