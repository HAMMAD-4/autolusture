import Link from 'next/link';import { SiteFooter,SiteNav } from '@/components/site-shell';import { services } from '@/lib/data';
export default function Home(){return <><SiteNav/><main><div className="wrap hero"><div><div className="eyebrow">Sydney’s considered detailers</div><h1>Your car, at its <em>most luminous.</em></h1><p>Quietly obsessive vehicle care for people who notice the small things. We bring every surface back to its best — without the fuss.</p><div className="hero-actions"><Link className="button dark" href="/booking">Book your detail →</Link><Link className="button" href="/services">Explore services</Link></div></div><div className="hero-image"><div className="result-tag">✦ <b>Completed today</b><br/>Paint correction · Mosman</div></div></div><section className="section"><div className="wrap"><div className="section-head"><div><div className="eyebrow">Our standard</div><h2>Care that feels<br/><em>unusually thorough.</em></h2></div><Link className="pill dark" href="/services">All services →</Link></div><div className="cards">{services.slice(0,3).map((s,i)=><Link className="card" href={`/services/${s.slug}`} key={s.slug}><span className="num">0{i+1} · {s.category}</span><h3>{s.name}</h3><p>{s.description}</p><span className="price">From ${s.price} · {s.duration} →</span></Link>)}</div></div></section><section className="section dark"><div className="wrap compare"><div className="compare-photo"/><div><div className="eyebrow" style={{color:'#b7ceca'}}>The AutoLustre approach</div><h2>Less showroom theatre.<br/><em>More real craft.</em></h2><p style={{color:'#bdc9c7',lineHeight:1.75,maxWidth:440}}>We work to a considered checklist, use professional-grade products, and never rush the final ten per cent. It is why our clients notice the difference before they get back in the driver’s seat.</p><div className="statgrid" style={{marginTop:25}}><div className="stat"><b>4.9/5</b><span>average client rating</span></div><div className="stat"><b>2,800+</b><span>vehicles restored</span></div></div></div></div></section><section className="section"><div className="wrap"><div className="cta"><div><div className="eyebrow">Your next reset</div><h2>Bring back that first-drive feeling.</h2></div><Link className="button dark" href="/booking">Make a booking →</Link></div></div></section></main><SiteFooter/></>}
